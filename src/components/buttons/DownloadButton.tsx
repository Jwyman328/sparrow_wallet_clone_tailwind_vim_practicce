import React from "react";

export const DownloadButton = () => {
  return (
    <button
      className="h-[13px] w-[13px] rounded-lg bg-gray-500"
      onClick={() => console.log("download button clicked")}
    />
  );
};
